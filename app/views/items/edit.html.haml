%h2 Edit item
%ul.add-bottom
  %li
    %a{ href: category_items_path(@category.id) }
      %i.icon-left-open Back to all items
= render('shared/error_messages', target: @item)
= form_for(@item, url: category_item_path(@category.id, @item.id)) do |f|
  = render(partial: 'form', object: f)